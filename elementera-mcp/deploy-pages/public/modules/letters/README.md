# public/modules/letters

Purpose: 予爱机书, 登岛信, letter drafts, and letter-specific rendering / storage boundaries.

Current state: the construction map tracks this as a named block, but active runtime ownership still needs confirmation before moving code.

Construction rule: letter features belong here. Do not attach letter behavior to Daily, Wolf Den, Serpent Desk, or `public/app.js`.

Before adding storage, document the key in `public/modules/storage`.
